<h1>{{title}}</h1>
{{#if user.savedPets.length}}
    <h3>Saved Pets</h3>
    <ul>
        {{#each user.savedPets}}
            <li><a href="/petdetails/{{this._id}}">{{this.name}}</a></li>
        {{/each}}
    </ul>
{{else}}
    <p>No pets saved</p>
{{/if}}